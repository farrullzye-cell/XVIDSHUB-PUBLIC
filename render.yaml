services:
  - type: web
    name: xvidshub-public
    runtime: node
    plan: free
    buildCommand: npm install
    startCommand: npm start
    autoDeploy: true
    previews:
      generation: automatic
